## About this project

This repository contains:

1. A **C++ console version** of Tic Tac Toe (`cpp/tictactoe.cpp`)
2. A **web version** built with HTML, CSS, and JavaScript

The web version recreates the same game logic in JavaScript
because C++ cannot run directly in a browser.
